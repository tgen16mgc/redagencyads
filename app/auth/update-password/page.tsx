import { WorkspacePasswordUpdate } from "@/components/workspace-password-update";

export const dynamic = "force-dynamic";

export default function UpdatePasswordPage() {
  return <WorkspacePasswordUpdate />;
}
